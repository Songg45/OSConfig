metricbeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false

fields:
  pipeline_source: metricbeat
fields_under_root: true

processors:
  - add_host_metadata: ~

output.logstash:
  hosts: ["172.20.200.200:30200"]

logging.level: info
